From the area options, select an appropriate area for the current task.

${agent} is heading to <${target_sector}>, which contains ${target_arenas}.
${daily_plan}
Question:
${agent} is ${complete_plan}. To ${decomposed_plan}, which area within ${target_sector} should ${agent} go to?

Requirements:
1. Must select an area from this list: [${target_arenas}].
2. If currently located in an area from the list and the planned activity can be performed there, it's best to stay in the current area.
3. Do not select areas outside the list.
4. Directly output the selected result.

${agent} should go to:
